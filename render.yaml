services:
  - type: web
    name: rasa-bot
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /status
    envVars:
      - key: PORT
        value: 5005
